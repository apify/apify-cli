import { defineMessages } from '../../../lib/i18n/index.js';

export const TelemetryEnableCommandMessages = defineMessages({
	en: {
		enabled: {
			markdown: 'Telemetry enabled.',
			json: () => null,
		},
		alreadyEnabled: {
			markdown: 'Telemetry is already enabled.',
			json: () => null,
		},
	},
});
