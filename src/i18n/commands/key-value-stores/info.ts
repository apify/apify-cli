import { defineMessages } from '../../../lib/i18n/index.js';

export const KeyValueStoresInfoCommandMessages = defineMessages({
	en: {
		storeNotFound: {
			markdown: 'Key-value store with ID or name "{storeId}" not found.',
			json: () => null,
		},
	},
});
