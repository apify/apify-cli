import { defineMessages } from '../../../lib/i18n/index.js';

export const RunsInfoCommandMessages = defineMessages({
	en: {
		runNotFound: {
			markdown: 'Run with ID "{runId}" was not found on your account.',
			json: () => null,
		},
	},
});
